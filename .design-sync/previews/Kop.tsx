import { Kop } from 'cura';

// Section heading — Lora italic, warm muted, sentence case. Sits just above a
// group of chips or fields inside a sheet.
export function Sectie() {
  return <Kop>Vandaag</Kop>;
}

export function Vraag() {
  return <Kop>In welke kamer?</Kop>;
}
